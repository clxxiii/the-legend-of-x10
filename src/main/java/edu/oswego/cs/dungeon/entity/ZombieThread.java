package edu.oswego.cs.dungeon.entity;

import edu.oswego.cs.dungeon.Entity;

public class ZombieThread extends Entity {

    public ZombieThread() {
        this.setHp(200);
        this.name = "Zombie Thread";
    }
}
